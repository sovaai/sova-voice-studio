import React, { memo } from 'react';

const Footer = memo(() => {
  return <footer className="flex items-center w-full p-2">FOOTER - @SOVA.AI</footer>;
});

Footer.displayName = 'Footer';

export default Footer;
